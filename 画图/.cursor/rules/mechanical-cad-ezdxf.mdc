---
description: 机械 CAD / ezdxf 出图工作流与模型选用（工业制图习惯）
alwaysApply: true
---

# 机械 CAD（ezdxf）出图规则

## 核心结论

- 手动选定模型后**不会**自动切换；只有聊天顶部选 **Auto** 才会动态换模型。
- 设置页的模型清单 = 启用/禁用列表，**不是**自动切换开关。
- CAD 场景**不要开 Auto**（易选到速度快但几何不稳的模型）。日常锁定 **Sonnet 5**；复杂半剖/多标注/精密零件用 **Opus 5**。
- **不要**用 Grok 做高精度机械图；**不要**用 Codex 5.3 / Composer 做复杂制图。

## 何时换模型有用 / 没用

**换模型往往能修好：** 坐标错、圆角错位、多段线不闭合、阵列/对称失败、图层线型标注混乱。

**换模型也救不了：** 需求模糊（只说“不好看”）、一条 prompt 塞全部结构、每次从零重写代码。必须拆分步骤 + 迭代修改。

## 强制工作流

1. 参数化：尺寸/坐标集中在代码顶部参数区；禁止魔法数散落。
2. 分层（GB 习惯）：`轮廓`（白/7，Continuous）、`中心线`（红/1，CENTER）、`标注`（绿/3）；需要时加 `剖面线`、`细实线`、`尺寸线`。
3. DXF：`R2013` 或 `R2010+`，单位 **mm**，兼容 AutoCAD / 浩辰 / 中望。
4. 多段线必须闭合；无重叠断线；线宽区分粗实线/细实线。
5. **增量修改**：后续调整必须保留原框架，只改缺陷清单；禁止全盘重写。
6. 复杂图拆步：轮廓 → 孔/板 → 倒角圆角 → 半剖剖面线 → 尺寸标注 → 明细引出。
7. 交付物优先 **可打开的 `.dxf`**，再用 CAD 微调出图；不要指望栅格 PNG“仿真 CAD”达到厂家图深度。
8. 工业半剖习惯：上半外形（螺栓可见）、下半剖视（ANSI31 剖面线）、中心线点划线、尺寸带箭头与引出线、零件球标。

## Prompt 习惯

- 首轮：明确单位、图层、闭合、参数区、不简化结构。
- 迭代：粘贴完整旧代码 + 逐条缺陷（最好附图标注），要求只改列出项。
- 参考模板：`templates/ezdxf_mech_template.py`

## 放空/限流孔板相关补充

- 放空孔板优先 **SY/T 10043 / API 521 + Campbell**，勿默认套 HG/T 稳态限流（除非业主明确要求）。
- 结构图要对齐工业半剖习惯；计算书与 DXF 分开迭代。
---

# 模板与迭代提示词（摘要）

首轮要求包含：单位 mm、R2013、规范分层、闭合多段线、顶部参数区、注释保留、不简化。

迭代要求包含：基于完整旧代码增量改、禁止重写框架、只修缺陷清单、输出完整可运行代码。
