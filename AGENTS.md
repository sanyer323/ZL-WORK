# 默认 Agent 约定

- 默认遵循 `owner-prefs`：懂人、懂我、积攒知识；白话结论先行。
- 重要/高成本任务默认 `dual-model-watch`：两版 + 主模型取舍，禁止机械互撕。
- FY301 / 定位器 / HART：同时遵循 `positioner-rd`。
- 可复用结论增量写入 Skill 或 `docs/kb`；禁止整篇重写。
- 分领域智库见 `domain-think-tank`：任务收尾入库；可用 Automation 定时喂料。
